# Copy to services/backend/.dev.vars for local development.
# Postgres provided by docker-compose (see docker-compose.yml).
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/odyssey

# Dashboard -> backend base URL (Expo web)
EXPO_PUBLIC_API_URL=http://localhost:8787
