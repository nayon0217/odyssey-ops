// @odyssey/api-client — public surface.
// Re-exports the Orval-generated hooks/models plus the shared fetch mutator.
// The generated barrel (./generated) is added once `gen:client` runs in Phase 1.
export * from './mutator';
